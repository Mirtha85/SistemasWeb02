<!DOCTYPE html>
<html lang="es">
<head>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.6.0.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="icon" type="image/x-icon" href="https://img.freepik.com/vector-premium/plantilla-diseno-logotipo-esport-mascota-zorro_382438-699.jpg?w=740">


    <script src="~/lib/jquery/jquery.js"></script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SistemasWeb01</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/cards.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/categoria.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/shoppingDetalle.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/productos.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/navCarrusel.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Tarjetas.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/barra_navegacion.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <link rel="stylesheet" href="~/SistemasWeb01.styles.css" asp-append-version="true" />
    <script src="https://unpkg.com/scrollreveal@4"></script>
    <script src ="~/js/site.js" asp-append-version="true"></script>
    <script src="~/js/barra_navegacion.js" asp-append-version="true"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>

</head>
<body>
    <header class="cabezera">
        <nav class="navbar">
            <div class="logo">Logo</div>
            <ul class="nav-links">
                <li><a asp-area="" asp-controller="Home" asp-action="Index">Inicio</a></li>
                <li><a asp-area="" asp-controller="Home" asp-action="About">Acerca</a></li>
                <li><a asp-area="" asp-controller="Producto" asp-action="Index">Productos</a>

                <li><a href="#">Contacto</a></li>
                @*<li>@ViewBag.cantidadItems</li> para recuperar la cantidad*@
                <partial name="_LoginPartial" />
            </ul>
@*            este es lo nuevo de search*@
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Producto" asp-action="Search">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                            <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z" />
                        </svg>
                    </a>
                </li>
            </ul>

            <div class="hamburger">
                <div class="line"></div>
                <div class="line"></div>
                <div class="line"></div>
            </div>
        </nav>
        
        <div class="menu">
            <ul class="menu-links">
                <li><i class="bi bi-house-fill"></i><a asp-area="" asp-controller="Home" asp-action="Index">Inicio</a></li>
                <li><i class="bi bi-people-fill"></i><a asp-area="" asp-controller="Home" asp-action="About">Acerca</a></li>
                <li><i class="bi bi-earbuds"></i><a asp-area="" asp-controller="Producto" asp-action="Index">Productos</a>
                <li><i class="bi bi-person-vcard-fill"></i><a href="#">Contacto</a></li>
                
            </ul>
            <hr/>
            <ul class="loginC menu-links">
                <partial name="_LoginPartial" />
            </ul>
            
        </div>

    </header>

    <div class="my-container">
        <main role="main" class="my-content">
            <!-- Contenido aquí -->
            @RenderBody()
            @RenderSection("Scripts", required: false)
        </main>
    </div>

    


    <script>
        // Función que verifica si el contenido ha llegado al final del render
        function isContentAtBottom() {
            return window.innerHeight + window.scrollY >= document.body.offsetHeight;
        }

        // Función para mostrar u ocultar el footer dependiendo de la posición del contenido
        function toggleFooterVisibility() {
            var footer = document.getElementById('footer21');
            if (isContentAtBottom()) {
                footer.style.display = 'block';
            } else {
                footer.style.display = 'none';
            }
        }

        // Asociar la función toggleFooterVisibility() al evento de desplazamiento (scroll)
        window.addEventListener('scroll', toggleFooterVisibility);
    </script>
    <script>
        //
        // Función que verifica si el contenido ha llegado al final del render
        //function isContentAtBottom() {
        //    console.log("llego al final");
        //    return window.innerHeight + window.scrollY >= document.body.offsetHeight;
        //}

        //// Función para mostrar u ocultar el footer dependiendo de la posición del contenido
        //function toggleFooterVisibility() {
        //    console.log("llego al final");
        //    var footer = document.querySelector('.f1');
        //    if (isContentAtBottom()) {
        //        footer.style.display = 'block';
        //    } else {
        //        footer.style.display = 'none';
        //    }
        //}
        //
        var seleccionado = false;
        //<!--esto es del click del menu hamburguesa-- >
        document.addEventListener('DOMContentLoaded', function () {
            const hamburger = document.querySelector('.hamburger');
            const menu = document.querySelector('.menu');

            hamburger.addEventListener('click', function () {
                menu.classList.toggle('show');
                console.log("Click en la hamburguesa")
                seleccionado = true;
            });
        });

        window.addEventListener('load', toggleNavVisibility);
        window.addEventListener('scroll', toggleNavVisibility);

        var prevScrollPos = window.pageYOffset;
        var scrollingUp = false;
        var nav = document.querySelector('.cabezera');
        var navHeight = nav.offsetHeight;
        var windowHeight = window.innerHeight;
        console.log(prevScrollPos + " fuera de la funcion");
        function toggleNavVisibility() {
            var currentScrollPos = window.pageYOffset;
            console.log(currentScrollPos);
            if (currentScrollPos != 0) {
                console.log("hola mundo");
                if (prevScrollPos > currentScrollPos) {
                    // Scroll hacia arriba
                    scrollingUp = true;
                    nav.classList.remove('abajo')
                    nav.classList.add('arriba');
                    console.log("arriba")
                    seleccionado = false;
                }
                else {
                    // Scroll hacia abajo
                    if (seleccionado == true) {

                    }
                    else {
                        scrollingUp = false;
                        nav.classList.remove('arriba');
                        nav.classList.add('abajo');
                        console.log("Abajo");
                    }

                }
            }
            prevScrollPos = currentScrollPos;
        }
    </script>
    <style>
        /*Scroll nav*/
        /*.nav-scroll {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            transition: transform 0.3s;
            z-index: 9999;
        }

        .nav-scroll.hide-nav {
            transform: translateY(-100%);
        }

        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            transition: transform 0.3s;
            z-index: 9999;
        }

        .navbar.hide-nav {
            transform: translateY(-100%);
        }

        .container-fluid {
            background-color:cornflowerblue;
        }
        
        .navegacion{
            padding:0px;
            margin:0px;
        }

        .navegacionNav {
            padding: 0px;
            margin: 0px;
        }
        *{
            margin:0px;
            padding:0px;
        }
        .my-content{
            width:100%;
        }*/
        .f1 {
            width: 100%;
            text-align: center;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
            background-color: rgba(49, 47, 47, 1);
            color: antiquewhite;
            position:sticky;
            display:none;
            bottom:0px;
        }
        .pFooter{
            margin:0px;
        }
        .my-container {
            max-width: 100%; /* Establecer el ancho máximo del contenedor */
            margin: 0px; /* Centrar el contenedor */
            padding: 0px; /* Agregar un espacio interno */
            margin-bottom:50px;
            margin-top:0px;
        }
    </style>
    
</body>
</html>
